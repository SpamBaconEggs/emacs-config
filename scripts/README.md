Auxilliary scripts used in my emacs configuration.

These bash/python scripts are used to interact with the SCM and build
system in a very specific environment, so they're not really useful in
other contexts.